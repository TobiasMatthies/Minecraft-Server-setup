services:
  mc-server:
    build: .
    container_name: mc-server
    ports:
      - '25565:25565'
    volumes:
      - ./data:/server/data
    tty: true
    stdin_open: true
    restart: unless-stopped
